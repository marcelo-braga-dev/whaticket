version: '3'

networks:
  whaticket:

services:

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    environment:
      - PMA_HOSTS=mysql
      - UPLOAD_LIMIT=150M
    ports:
      - ${PMA_PORT:-9000}:80
    networks:
      - whaticket